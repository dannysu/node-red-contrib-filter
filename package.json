{
  "name": "node-red-contrib-filter",
  "version": "0.1.4",
  "description": "A Node-RED node to filter message(s)",
  "license": "Apache-2.0",
  "keywords": [
    "node-red",
    "filter",
    "switch",
    "if"
  ],
  "node-red": {
    "nodes": {
      "filter": "18-filter.js"
    }
  },
  "author": {
    "name": "Danny Su",
    "email": "contact@dannysu.com"
  },
  "scripts": {
    "setup": "rm -f user/node_modules/node-red-contrib-filter && cd user/node_modules && ln -sf ../../ node-red-contrib-filter",
    "nodered": "node-red -u user"
  },
  "devDependencies": {
    "node-red": "^1.3.5"
  }
}
